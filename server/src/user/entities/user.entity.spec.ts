import { UserEntity } from './user.entity';

describe('UserEntity', () => {
  // TODO: improve this test, should check if class have fields
  it('should be defined', () => {
    expect(UserEntity).toBeDefined();
  });
});
